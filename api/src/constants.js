const GAMES_URL = `https://api.rawg.io/api/games`;
const GENRES_URL = `https://api.rawg.io/api/genres`;
const GENRES_ID = `https://api.rawg.io/api/games/`;


module.exports = {
    GAMES_URL,
    GENRES_URL, 
    GENRES_ID
}